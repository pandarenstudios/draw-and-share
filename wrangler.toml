name = "worker-old-flower-7fca"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
GITHUB_OWNER = "pandarenstudios"
GITHUB_REPO  = "draw-and-share"

[[kv_namespaces]]
binding = "BANNED_IPS"
id = "91fc89a0cc8047b2a28118209be8252a"  # paste your KV namespace ID here — see setup instructions below
# 1. Go to Cloudflare dashboard → Workers & Pages → KV
# 2. Click "Create namespace", name it BANNED_IPS
# 3. Copy the ID and paste it between the quotes above
# 4. Run: npx wrangler deploy
